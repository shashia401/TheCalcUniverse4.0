// Backwards-compatible re-export. The registry now lives in ./registry/<category>.ts files.
  // New calculators: add to the matching per-category file under ./registry/, NOT here.
  export * from './registry/index';
  